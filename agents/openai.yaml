interface:
  display_name: "Universal Public Data Pipeline"
  short_description: "Design datasets and prove public data paths before scraping"
  default_prompt: "Use $universal-public-data-pipeline to design the dataset need, prove the public data path, score feasibility, identify trapdoors, and plan a reusable pipeline before any full collection."
