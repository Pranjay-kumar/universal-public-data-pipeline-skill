interface:
  display_name: "Universal Public Data Pipeline"
  short_description: "Run public data due diligence before building scrapers"
  default_prompt: "Use $universal-public-data-pipeline to prove the public data path, score feasibility, identify trapdoors, and plan a reusable pipeline before any full collection."
