interface:
  display_name: "Universal Data Acquisition Pipeline"
  short_description: "Design robust scraping and API data pipelines"
  default_prompt: "Use $universal-data-acquisition-pipeline to classify source access, design the dataset need, prove the data path, score feasibility, identify trapdoors, and plan a production-grade acquisition pipeline."
